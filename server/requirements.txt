# NetMonitor Pro — 服务端 Python 依赖 (优化版)
# 文件位置: server/requirements.txt
#
# 优化点:
#   1. 文件名从 "requirements" 改为 "requirements.txt"（原文件缺后缀，pip 找不到）
#   2. 新增 flask-limiter 速率限制依赖
#   3. 新增 gunicorn 生产部署 WSGI 服务器
#   4. 锁定最低版本号，保证兼容性

Flask>=3.0.0
PyJWT>=2.8.0
requests>=2.31.0
gunicorn>=21.2.0
flask-limiter>=3.5.0